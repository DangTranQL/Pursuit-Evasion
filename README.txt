Install all neeeded libraries: pip install -r requirements.txt

If you want to train the model again, run: py train.py

If not, to test the algorithm with the q_tables we already have, run: py test.py

To visualize how the pursuers catch the evader with our Q-learning, run: py main_results.py